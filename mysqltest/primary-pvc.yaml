Kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  annotations :
    trident.netapp.io/shareToNamespace: payroll
  name: mysql-pvc
  namespace: argocd
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: trident
  resources:
    requests:
      storage: 1Gi
